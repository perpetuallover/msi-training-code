package com.mercury.oop;

public class Base2 {
	//default constructor is the one with no parameter, system will provide one if user not create it manually(only there is no constructor)
	// ?
	public Base2(){}; // industry use default way
	public Base2(String str){
		System.out.println("Base2 in " + str);
	}
}
